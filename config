PROJECT=openNV


CC=clang++
LIBCPP=/home/bojan/Documents/untar_pit/clang
INCLUDEFLAGS = $(foreach d, $(INCLUDEDIRS), -I$d)
CFLAGS := --std=c++17 $(INCLUDEFLAGS) #-nostdinc++ -I$(LIBCPP)/include/c++/v1    -DLOG_USE_COLOR $(INCLUDEFLAGS) -Wall -MD

LDFLAGS= -L$(LIBDIR) -lz # -stdlib=libc++  -lc++abi   -L$(LIBCPP)/lib  -Wl,-rpath,$(LIBCPP)/lib
